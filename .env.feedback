# 反馈数据收集配置示例
# 复制此文件为 .env.local 并填入真实的配置值

# Formspree 表单服务 (免费，推荐)
# 1. 访问 https://formspree.io/ 注册账号
# 2. 创建新表单，获取表单ID
# 3. 填入下方配置
REACT_APP_FORMSPREE_ID=YOUR_FORMSPREE_ID

# 自定义反馈API端点 (可选)
# 如果有自己的后端服务，可以配置此项
REACT_APP_FEEDBACK_API=https://api.yourdomain.com/feedback

# GitHub Issues 集成 (可选)
# 用于将反馈自动创建为GitHub Issues
REACT_APP_GITHUB_TOKEN=your_github_personal_access_token
REACT_APP_GITHUB_REPO=bytedesk/bytedesk

# Google Analytics 4 (可选)
# 如果已配置GA4，反馈数据会自动发送到GA4
# 无需额外配置，系统会自动检测gtag函数

# 邮件通知 (可选，需要配合Formspree或自定义API使用)
REACT_APP_FEEDBACK_EMAIL=feedback@yourdomain.com
